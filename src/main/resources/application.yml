spring:
  application:
    admin:
      enabled: true
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/albamtest
    username: root
    password: wnsdn13
  jpa:
    show-sql: true
    database : mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: false
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate: info
    root: info
    org.springframework.web: debug